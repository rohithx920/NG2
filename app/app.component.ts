import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <!--<event-list></event-list> -->
    hi
  `
})
export class AppComponent {
  name = 'app works!';
}